# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/.terraform.lock.hcl
terraform/terraform.tfvars
terraform/id_ed25519
terraform/id_ed25519.pub

# Credential management — age-encrypted backups and deploy keys
*.age
*.age.sha256
*-deploy-key
*-deploy-key.pub
.age-recovery-key.txt
terraform/terraform.tfvars.bak

# Bot assets (not part of infrastructure)
giskard/

# direnv
.envrc

# Playwright MCP (browser automation logs)
.playwright-mcp/

# OS
.DS_Store
